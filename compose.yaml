# Generated by .brand/gen_repo_compose.py — regenerate rather than hand-edit.
#
#   infisical export --projectId techyst-08-plane --env prod --format dotenv > .env
#   docker compose up -d
#
# Images come from ghcr.io; `docker login ghcr.io` first. Databases, cache,
# queue and object storage are all external, so nothing here holds state and
# `docker compose down` is safe.

name: techyst-08-plane
services:
  migrator:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-plane-migrator:${TAG:-latest}
    env_file:
    - ./.env
    restart: 'no'
    pull_policy: always
    command:
    - ./bin/docker-entrypoint-migrator.sh
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  api:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-plane-api:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - ./bin/docker-entrypoint-api.sh
    ports:
    - 8030:8000
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  worker:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-plane-worker:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - ./bin/docker-entrypoint-worker.sh
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  beat-worker:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-plane-beat-worker:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - ./bin/docker-entrypoint-beat.sh
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  live:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-plane-live:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    ports:
    - 8031:3000
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
